<%= form_with(model: child, local: true) do |form| %>

  <% if child.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(child.errors.count, "error") %> prohibited this child from being saved:</h2>

      <ul>
        <% child.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :child_name %>
    <%= form.text_field :child_name %>
  </div>

  <div class="field">
    <%= form.label :child_number %>
    <%= form.number_field :child_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
