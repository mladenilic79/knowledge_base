
<%# showcase static method & scopes instead of parameter in controller %>
<%# (routes, models/blogs, controller/blogs_controller, views/custom_methods_from_model) %>

<p id="notice"><%= notice %></p>

<h1>blogs</h1>

<table>
  <thead>
  <tr>
    <th>blog name</th>
    <th>blog number</th>
    <th>parent id</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @classic_method.each do |blog| %>
    <tr>
      <td><%= blog.blog_name %></td>
      <td><%= blog.blog_number %></td>
      <td><%= blog.parent_id %></td>
      <td><%= link_to 'Show', blog %></td>
      <td><%= link_to 'Edit', edit_blog_path(blog) %></td>
      <td><%= link_to 'Destroy', blog, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>

<table>
  <thead>
  <tr>
    <th>blog name</th>
    <th>blog number</th>
    <th>parent id</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @scope.each do |blog| %>
    <tr>
      <td><%= blog.blog_name %></td>
      <td><%= blog.blog_number %></td>
      <td><%= blog.parent_id %></td>
      <td><%= link_to 'Show', blog %></td>
      <td><%= link_to 'Edit', edit_blog_path(blog) %></td>
      <td><%= link_to 'Destroy', blog, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>

<table>
  <thead>
  <tr>
    <th>blog name</th>
    <th>blog number</th>
    <th>parent id</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @scopeid.each do |blog| %>
    <tr>
      <td><%= blog.blog_name %></td>
      <td><%= blog.blog_number %></td>
      <td><%= blog.parent_id %></td>
      <td><%= link_to 'Show', blog %></td>
      <td><%= link_to 'Edit', edit_blog_path(blog) %></td>
      <td><%= link_to 'Destroy', blog, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<%= link_to 'New blog', new_blog_path %>